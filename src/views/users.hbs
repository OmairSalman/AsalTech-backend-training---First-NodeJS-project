{{!-- filepath: c:\Users\omair\Desktop\Omair\coding projects\asal-train\nodejs-test\src\views\users.hbs --}}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Users</title>
  <style>
    body {
      background: #f5f6fa;
      font-family: 'Segoe UI', Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    .users-container {
    background: #fff;
    padding: 2rem 2.5rem;
    border-radius: 12px;
    box-shadow: 0 4px 24px rgba(44, 62, 80, 0.08);
    width: 400px;
    max-height: 70vh;      /* Set a max height */
    overflow-y: auto;      /* Enable vertical scrolling */
  }
    h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #2d3436;
    }
    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    li {
      padding: 1rem 0;
      border-bottom: 1px solid #dfe6e9;
      color: #636e72;
    }
    li:last-child {
      border-bottom: none;
    }
    .user-name {
      font-weight: 600;
      color: #2d3436;
    }
    .user-email {
      font-size: 0.98rem;
      color: #636e72;
    }
    .home-btn {
      width: 100%;
      padding: 0.8rem;
      margin-bottom: 1rem;
      background: #00b894;
      color: #fff;
      border: none;
      border-radius: 6px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s;
    }
    .home-btn:hover {
      background: #55efc4;
    }
  </style>
</head>
<body>
  <div class="users-container">
    <form method="GET" action="/">
      <button type="submit" class="home-btn">Home</button>
    </form>
    <h2>All Users</h2>
    <ul>
      {{#each users}}
        <li>
          <span class="user-name">{{this.name}}</span><br>
          <span class="user-email">{{this.email}}</span>
        </li>
      {{else}}
        <li>No users found.</li>
      {{/each}}
    </ul>
  </div>
</body>
</html>